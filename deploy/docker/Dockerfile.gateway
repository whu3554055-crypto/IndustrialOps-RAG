# Gateway + ingest + 评测 CLI（Helm / 本地 build）
FROM python:3.11-slim-bookworm
WORKDIR /app
COPY . .
RUN pip install --no-cache-dir -e ".[dev,storage,doc]"
EXPOSE 8080
CMD ["uvicorn", "apps.gateway.main:app", "--host", "0.0.0.0", "--port", "8080"]
